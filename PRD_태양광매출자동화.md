# PRD: 태양광 매출 청구 및 데이터 정리 자동화 웹앱

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-25 |
| 작성자 | (담당자) |
| 상태 | Draft |

---

## 1. 개요 및 배경

### 1.1 배경
태양광 발전소(현재 최소 1개소 ~ 최대 40개소, 향후 지속 증가 가능)의 SMP·REC 매출 청구 및 세금계산서 발행 업무를 수기(엑셀 수작업)로 처리하고 있음. 이 과정에서 아래와 같은 문제가 반복적으로 발생함.

- 반복 작업(데이터 정리, 계산)에 과도한 시간 소요
- 수작업 과정에서 계산·입력 오류 발생 위험
- 세금계산서 발행 프로세스(마스터 정보 재사용, 배치 분할 등)가 번거롭고 실수 유발 소지가 큼

### 1.2 목표
1. SMP 매출 데이터 처리(메일 확인→PDF 정리→세금계산서 발행 파일 생성) **자동화**
2. 발전소별 · 통합 매출 데이터를 한눈에 확인할 수 있는 **정리된 뷰 제공**
3. REC 매출 데이터를 손쉽게 **입력·관리**할 수 있는 구조 제공
4. 발전소 개수 증감에 유연하게 대응 가능한 **확장 가능한 구조** 설계

### 1.3 범위(Out of Scope 명시)
- REC는 세금계산서 발행은 물론, **홈택스 업로드용 파일 생성도 하지 않음** (REC는 수량·단가 데이터 정리·조회까지만 지원)
- SMP는 홈택스/이자셈홈택스 실제 업로드·발행 행위는 범위 외 (업로드용 엑셀 파일 생성까지만 지원)
- 과거 이력 데이터(2015~2026) 마이그레이션은 범위 외 (기존 엑셀 파일로 별도 보관, 신규 시스템은 향후 데이터부터 축적)

---

## 2. 사용자 및 사용 환경

| 항목 | 내용 |
|---|---|
| 사용자 | 1인 사용 (MVP) → 추후 팀 단위로 확장 고려 |
| 사용 환경 | **1단계**: 로컬(내 PC)에서 사용 → **2단계**: 클라우드 배포로 언제 어디서든 접속 가능하도록 확장 |
| 관리 대상 규모 | 거래처(발전소 그룹) 기준 최소 1개소 ~ 최대 40개소, 향후 증가 가능 → **발전소 개수에 유연한 구조 필수** |

---

## 3. 용어 정의

| 용어 | 설명 |
|---|---|
| SMP | 계통한계가격(System Marginal Price) 기반 전력 판매 매출 |
| REC | 신재생에너지 공급인증서(Renewable Energy Certificate) 판매 매출 |
| 계약번호 | 한전이 발전소(계약) 단위로 부여하는 고유번호 (예: 5000070051) |
| 종사업장번호 | 세금계산서 발행 시 한전 측에 필수 입력되는 사업장 구분 코드 (발전소별로 상이) |
| 발전소 별칭 | 세금계산서 "비고"란에 사용하는 내부 관리용 발전소 짧은 이름 (한전 공식명과 다를 수 있음) |
| 확정 | 해당 월의 SMP·REC 청구가 모두 완료된 상태 (SMP만 완료 시 "SMP확정", REC까지 완료 시 "확정") |

---

## 4. 핵심 업무 프로세스

### 4.1 SMP 프로세스 (1순위 자동화 대상)

```
① 매월 중순, 한전에서 발전소(계약번호)별 "신재생에너지 요금안내" 메일 다수 수신
   ※ 개수는 발전소 수에 따라 유동적 (고정 아님)
   ※ 중요: 메일 수신월 ≠ 발전월. 메일은 항상 "전월 발전분"에 대한 내용임
        (예: 8월에 받은 메일 → 7월 발전분 청구)

② 웹앱이 **IMAP 연동**을 통해 메일함을 자동 확인(다음 메일, 일정 주기로 폴링)
   → 대상 메일을 자동으로 PDF로 변환·저장
   - 사용자 개입 없이 자동 수집 (수동 저장/업로드 불필요, MVP부터 포함)
   - PDF 본문에서 발전소명을 자동 인식하여 파일명/데이터 매칭에 활용
   - PDF에서 아래 데이터 자동 추출:
     발전소명, 주소, 용량(kW), 발전기간(귀속월 판단 기준),
     전월지침/당월지침/차감지침/계기배수/발전량,
     기준단가/SMP단가/손실단가, 공급가액, VAT

②-1 예외 처리 (자동 처리 실패 시)
   - PDF 파싱 실패(양식 상이, 항목 누락 등) 또는 발전소명 자동 매칭 실패 건은
     "검토 필요" 상태로 별도 목록에 표시
   - 사용자가 해당 건을 확인하여 발전소를 수동 지정하거나 추출값을 직접 보정 가능
   - 이 목록이 비워지지 않으면 해당 월 청구가 누락될 수 있음을 대시보드에서 강조 표시

③ 거래처 전달은 **사용자가 웹앱에서 직접 진행**
   - 사용자가 웹앱에서 원하는 발전소(들)의 PDF를 선택해 원하는 폴더로 다운로드
   - 다운로드한 PDF를 사용자가 직접 이메일로 거래처에 전송 (자동 발송은 하지 않음)

④ 세금계산서 발행 준비
   - 추출된 SMP 데이터(공급가액, 세액) + 발전소 마스터 정보
     (종사업장번호, 담당 이메일, 비고용 별칭, 공급받는자 사업자정보) 자동 결합
   - 발전소 "건설순서" 고정 기준으로 최대 10건씩 배치 분할하여
     한전 세금계산서 등록양식(.xls) 자동 생성

⑤ 사용자가 생성된 파일을 홈택스/이자셈홈택스에 직접 업로드하여 일괄 발행
   - 업로드/발행 자체는 시스템이 알 수 없으므로, 사용자가 웹앱에서
     배치 단위로 "발행완료" 여부를 수동 체크 → tax_invoice_status 갱신
   ⏰ 마감: 통상 25일 전후 — 미발행 시 대금 미지급 + 가산세 위험
   - 마감일 기준 D-day 카운트다운 및 "미발행 배치" 경고를 대시보드 상단에 상시 노출
```

### 4.2 REC 프로세스

```
① 매월, 발전소별로 REC 현물시장 거래 발생 (발전소마다 수량·단가 상이)
② 사용자가 "대표(기준) 단가" 1개를 입력 → 전체 발전소에 기본값으로 자동 적용
③ 발전소별 REC 판매 수량은 사용자가 직접 입력
④ 필요 시 발전소별로 단가를 개별 수정 가능
⑤ (세금계산서 발행은 현물시장/홈택스에서 별도 처리 — 본 웹앱 범위 아님)
```

### 4.3 데이터 정리 및 리포트 프로세스

```
① SMP 데이터 + REC 데이터를 발전소·월 단위로 결합
② 발전소별 뷰 / 거래처(회사)별 뷰 / 전체 통합 뷰 제공
③ 청구 진행상태 표시: 미청구 → SMP확정 → 확정(SMP+REC 모두 완료)
④ 엑셀 다운로드 지원 (기존 업무 방식과의 호환성 유지)
```

---

## 5. 기능 요구사항 (우선순위)

### Phase 1 — SMP 자동화 (1순위, MVP 핵심)
| 기능 | 설명 |
|---|---|
| 발전소 마스터 관리 | 발전소명, 계약번호, 종사업장번호, 별칭, 주소, 용량, 담당 이메일, 공급받는자(소유주) 사업자정보(사업자번호/상호/주소/업태/종목/이메일), 건설순서 CRUD |
| 한전 메일 IMAP 자동 수집 | IMAP 연동으로 메일함을 자동 확인 → 대상 메일 자동 탐지 → PDF로 자동 변환·저장 (수동 업로드 불필요) |
| SMP PDF 자동 파싱 | 자동 수집된 PDF에서 발전소명 자동 인식 → 핵심 데이터(발전량, 단가, 공급가액, VAT, 발전기간) 자동 추출 |
| 귀속월 자동 판단 | PDF 내 "발전기간"을 기준으로 매출 귀속월 자동 결정 (메일 수신월과 구분) |
| PDF 다운로드(거래처 전달용) | 사용자가 원하는 발전소(들)의 PDF를 선택해 원하는 폴더에 다운로드 → 이후 사용자가 직접 이메일 발송 |
| 세금계산서 등록양식 자동생성 | 추출 데이터 + 발전소 마스터 정보를 결합해 한전 지정 .xls 양식대로 자동 채움 |
| 배치 분할 (10건 단위) | 발전소 건설순서 고정 기준으로 최대 10건씩 자동 분할하여 파일 여러 개 생성 (1차, 2차...) |
| 파싱·매칭 실패 검토 | PDF 파싱 실패 또는 발전소명 매칭 실패 건을 별도 목록으로 표시, 수동 보정/재매칭 지원 |
| 발행완료 처리 | 배치 단위로 홈택스 업로드 완료 여부를 사용자가 수동 체크 → tax_invoice_status 갱신 |
| 마감 알림 | 세금계산서 마감일까지 D-day 카운트다운 및 미발행 배치 경고를 대시보드에 상시 노출 |

### Phase 2 — 통합 매출 데이터 리포트
| 기능 | 설명 |
|---|---|
| 발전소별 뷰 | 발전소 1개 선택 시 월별(발전량·발전시간·SMP단가·SMP금액·REC수량·REC금액·매출총액) 추이 표 |
| 거래처별/통합 뷰 | 여러 발전소를 회사 단위 또는 전체로 묶어 월별 매출 합계 대시보드 |
| 청구 상태 표시 | 미청구/SMP확정/확정 상태를 시각적으로 구분 |
| 엑셀 내보내기 | 현재 사용 중인 리포트 양식과 호환되는 형태로 다운로드 지원 |

### Phase 3 — REC 데이터 관리
| 기능 | 설명 |
|---|---|
| 대표 단가 입력 | 월 1회 대표(기준) REC 단가 입력 → 전체 발전소 기본 적용 |
| 발전소별 수량 입력 | 발전소별 REC 판매 수량 입력 폼 |
| 발전소별 단가 수정 | 필요 시 개별 발전소 단가만 수동 오버라이드 |

### Phase 4 — 확장 (후순위)
| 기능 | 설명 |
|---|---|
| 클라우드 배포 | 로컬 → 웹 호스팅으로 전환, 어디서든 접속 가능 |
| 거래처 자동 리포트 발송 | 이메일 자동 전송 기능 |
| 다중 사용자/팀 협업 | 사용자 계정·권한 관리 |

---

## 6. 데이터 모델 (초안)

### 6.1 발전소 마스터 (PlantMaster)
```
plant_id (PK), plant_name(공식명), plant_alias(비고용 별칭),
contract_number(한전 계약번호), sub_biz_number(종사업장번호),
kepco_contact_email(ppa0XXX@kepco.co.kr),
address, capacity_kw, construction_order(배치순서 기준),
client_group(거래처명, 예: 키스트론),
owner_biz_number, owner_name, owner_address, owner_biz_type, owner_biz_item,
owner_email1, owner_email2,
created_at, updated_at
```

### 6.2 월별 SMP 데이터 (SmpMonthly)
```
id (PK), plant_id (FK), billing_year_month(귀속월, 발전기간 기준),
UNIQUE(plant_id, billing_year_month),
received_date(메일 수신일),
prev_reading, curr_reading, meter_multiplier,
generation_kwh, base_unit_price, smp_unit_price, loss_unit_price,
supply_amount(공급가액), vat_amount(세액),
parse_status(정상/검토필요 — PDF 파싱·발전소 매칭 실패 시 검토필요),
tax_invoice_status(미발행/발행완료), batch_id (FK → InvoiceBatch, nullable),
source_pdf_path(파일 저장 위치 키 — 로컬 경로 또는 향후 클라우드 스토리지 키)
```

### 6.3 월별 REC 데이터 (RecMonthly)
```
id (PK), plant_id (FK), billing_year_month,
UNIQUE(plant_id, billing_year_month),
quantity(수량), unit_price(단가, 기본값=대표단가 상속·수정가능),
amount(자동계산: 수량*단가), status(잠정/확정)
```

### 6.4 REC 대표 단가 이력 (RecMonthlyDefault)
```
billing_year_month (PK), base_unit_price(대표 단가), created_at
```
※ `RecMonthly.unit_price`는 이 값을 기본 상속받되, 발전소별로 개별 오버라이드 가능. 월별 대표 단가 자체의 변경 이력을 남기기 위해 별도 테이블로 분리.

### 6.5 세금계산서 배치 (InvoiceBatch)
```
batch_id (PK), billing_year_month, batch_no(1차/2차...),
generated_file_path(파일 저장 위치 키), created_at
```
※ 포함 발전소 목록은 `SmpMonthly.batch_id` 역참조로 조회 (배치당 최대 10건). `InvoiceBatch`에 발전소 목록을 별도로 들고 있지 않도록 하여 양방향 참조로 인한 정합성 불일치를 방지.

---

## 7. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 확장성 | 발전소 개수 증감(현재 1~40개소)에 구조적 제약 없이 대응 |
| 데이터 정확성 | 세금계산서 필수 입력값(사업자번호, 종사업장번호 등) 누락 시 저장/생성 차단 |
| 파일 호환성 | 생성 파일은 기존 한전 지정 .xls 양식과 100% 동일한 컬럼 구조 유지 |
| 보안 | 사업자번호 등 민감정보 포함 — 1단계(로컬)에서도 접근 통제 고려, 클라우드 전환 시 인증 필수. IMAP 앱 비밀번호 등 자격증명은 DB에 평문 저장하지 않고 환경변수/OS 자격증명 관리자 등으로 분리 보관 |
| 이력 관리 | 과거 발행 이력(누가 언제 어떤 배치를 생성했는지) 추적 가능 |

---

## 8. 성공 지표 (KPI)

| 지표 | 목표 |
|---|---|
| SMP 데이터 처리 시간 | PDF 업로드~세금계산서 파일 생성까지 소요 시간 단축 (기존 대비 목표 % 추후 설정) |
| 입력 오류 발생 건수 | 세금계산서 발행 오류(재발행) 건수 감소 |
| 마감 준수율 | 마감일 초과로 인한 가산세/대금 미지급 발생 0건 |

---

## 9. 기술 스택 (제안)

### 9.1 애플리케이션 / 백엔드
| 영역 | 추천 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (React + TypeScript) | 프론트+백엔드(API Routes)를 한 언어로 처리, 로컬 실행 → 클라우드 배포(Vercel 등) 전환이 매끄러움 |
| DB | SQLite (1단계) → PostgreSQL/Supabase (2단계) | Prisma ORM 사용 시 DB 전환이 설정 변경만으로 가능. 1단계는 설치 부담 없는 SQLite로 시작 |
| ORM | Prisma | 발전소 마스터, SMP/REC 월별 데이터 등 데이터 모델을 스키마로 관리 |
| 메일 연동(IMAP) | `imapflow` (Node.js) | 다음(Daum) 메일 IMAP 지원 — 앱 비밀번호 발급 후 연동, MVP부터 적용 |
| PDF 파싱 | `pdf-parse` 또는 `pdfjs-dist` | 한전 PDF가 고정 양식이므로 텍스트 추출 + 정규식 기반 파싱으로 충분 |
| 엑셀 생성 | `exceljs` | 한전 지정 .xls 세금계산서 양식의 컬럼 구조·서식을 정확히 재현 |
| 배포(2단계) | Vercel(앱) + Supabase(DB) | 소규모 개인 프로젝트에 적합한 비용/운영 부담 |

### 9.2 UI/UX
| 영역 | 추천 | 이유 |
|---|---|---|
| 스타일링 | Tailwind CSS | Next.js와 표준 조합, 빠른 커스텀 스타일링 |
| 컴포넌트 라이브러리 | shadcn/ui | 테이블·폼·모달 등 데이터 중심 화면에 필요한 컴포넌트를 코드베이스에 직접 포함해 커스터마이징 |
| 데이터 테이블 | TanStack Table | 발전소별/통합 매출 데이터처럼 월별 컬럼이 많고 정렬·필터가 필요한 표에 최적화 |
| 차트 | Recharts | 월별 매출 추이, 발전소 간 비교 시각화 |
| 폼 처리 | React Hook Form + Zod | REC 수량·단가, 발전소 마스터 입력 시 유효성 검사 — "필수값 누락 시 저장 차단" 요구사항 구현 |
| 아이콘 | lucide-react | shadcn/ui와 기본 궁합 |

**디자인 방향**: 업무용 데이터 도구이지만 오늘의집처럼 **차분하고 포근한 블루 톤**을 지향한다.
- 컬러: 소프트 블루 계열 배경(연한 하늘색) + 진한 네이비 포인트 컬러(활성 메뉴, 강조 카드 등에 사용)
- 폰트: Pretendard (둥글고 친근한 한글 UI 폰트)
- 카드형 레이아웃 + 넉넉한 radius(12~16px), 진한 그림자 대신 은은한 soft shadow
- 넉넉한 여백으로 숫자·표가 많아도 답답하지 않게 구성
- 빈 상태·완료 상태 등에 손그림풍 일러스트/이모지 활용, 친근한 존댓말 마이크로카피 사용
- 숫자 가독성은 유지하되(고정폭 폰트 등), 딱딱한 ERP 느낌은 지양

### 9.3 화면 구조 및 내비게이션

- **구조**: 상단 헤더에 메뉴 버튼(예: 대시보드 / 발전소관리 / SMP / REC / 리포트 / 세금계산서)을 배치하고, 버튼 클릭 시 **완전히 새로운 화면(페이지)으로 전환**되는 구조로 구현한다.
  - 탭 전환이나 아코디언처럼 한 화면 안에서 내용만 접었다 펴는 방식은 지양한다.
  - 각 화면은 하나의 명확한 목적(예: "발전소 마스터 관리 화면", "SMP 월별 데이터 화면")만 담당하도록 분리한다.
- **가독성 최우선 원칙**:
  - 화면당 정보량을 적절히 제한하고, 관련 없는 정보를 한 화면에 욱여넣지 않는다.
  - 표(테이블)는 컬럼 수가 많아지더라도 헤더 고정(sticky header), 숫자 정렬(우측 정렬), 충분한 행간을 적용해 한눈에 읽히도록 한다.
  - 현재 위치를 항상 알 수 있도록 헤더에 활성 메뉴 강조 표시(현재 화면 하이라이트)를 적용한다.
  - 중요 정보(마감월, 청구 상태 등)는 시각적 위계(크기·색상·굵기)를 통해 눈에 먼저 띄도록 배치한다.


---

## 10. 열린 이슈 (추후 논의 필요)

- [ ] 한전 PDF 양식이 발전소 유형(고정가/영세율 등)에 따라 다를 가능성 → 추가 샘플 PDF 확보 필요
- [ ] 세금계산서 마감일이 "며칠 전"인지 발전소별로 정확히 동일한지 재확인
- [ ] REC 데이터 확정 처리 방식(누가 언제 "확정" 표시를 남기는지) 구체화
- [ ] 클라우드 전환 시점 및 예산/보안 요구수준
- [ ] 한전 세금계산서 등록양식(.xls) 및 기존 리포트 엑셀 양식의 실제 샘플 파일 확보 필요 (컬럼 구조를 동일하게 재현하기 위한 선행 조건)
- [ ] KPI 측정을 위한 baseline(현재 수작업 소요 시간, 재발행 건수 등) 수집 방법 필요
- [ ] 한전 메일이 수신되는 실제 메일 계정/서비스 확정 및 IMAP 폴링 주기 결정
- [ ] Phase 4 다중 사용자 확장을 대비해 이력 관리(발행 이력 등)에 사용자 식별자를 지금부터 스키마에 반영할지 결정

---

*이 문서는 초안이며, 실제 개발 착수 전 세부 화면 설계(와이어프레임) 및 기능 명세를 추가로 진행합니다.*
