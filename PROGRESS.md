# 개발 진행 체크리스트

> PRD_태양광매출자동화.md §5 기능 요구사항 기준. 진행하면서 이 파일을 계속 갱신합니다.

## Phase 1 — SMP 자동화 (MVP 핵심)

- [x] 프로젝트 뼈대: Next.js(TS) + Tailwind + shadcn/ui
- [x] DB 스키마: Prisma + SQLite (PRD §6 데이터 모델 반영)
- [x] 공통 UI: 상단 내비게이션(대시보드/발전소관리/SMP/REC/리포트/세금계산서), 디자인 톤(소프트 블루+네이비, Pretendard)
- [x] 발전소 마스터 관리 CRUD (목록/등록/수정/삭제, 필수값 검증)
- [x] 거래처(공급자) 관리 CRUD — 실제 세금계산서 양식 분석으로 새로 발견된 구조(§아래 참고)
- [x] 키스트론 31개 발전소 일괄 등록 완료 (계약번호 미확보 30건은 `PENDING-{순번}` 임시값 — 추후 실제 계약번호로 수정 필요)
- [x] 한전 메일 IMAP 자동 수집 — 다음 메일(imap.daum.net) 연동 완료, 전체 폴더에서 이번 달분 자동 탐지
- [x] SMP 메일 자동 파싱 — 실제 메일 HTML을 직접 파싱(발전소명/주소/용량/발전기간/지침/단가/공급가액/VAT 등). PDF 텍스트 추출 대신 메일 원문을 직접 파싱해 정확도를 높임
- [x] 귀속월 자동 판단 (발전기간 시작월 기준)
- [x] 파싱·매칭 실패 검토 화면 (`/smp` 페이지, 발전소 수동 연결 지원)
- [x] PDF 다운로드(거래처 전달용) — `/smp` 표에서 발전소별 다운로드 아이콘 클릭, Pretendard 폰트로 한글 정상 표시. 계약번호 미확보(`PENDING-*`) 발전소는 거래처용 문서에 내부 임시값이 노출되지 않도록 해당 줄 자체를 숨김
- [x] 세금계산서 등록양식 자동생성 — `/invoices` 페이지. 실제 2026-07 데이터로 생성해 과거 실제 파일과 값 전부 일치 확인(작성일자 제외)
- [x] 배치 분할(10건 단위) — 거래처·건설순서 기준 자동 분할 (2026-07: 31개 발전소 → 4개 배치)
- [x] 발행완료 처리(수동 체크) — 배치 단위 "발행완료 처리" 버튼
- [x] 마감 알림(D-day 카운트다운) — 대시보드 상단에 위젯으로 표시. 미발행 건수와 마감(25일) 기준 D-day/지남 여부를 실시간 반영 (revalidatePath 연결)

**Phase 1 핵심 기능 전부 구현 완료.** (2026-08-27 기준: 마감이 지났고 미발행 31건이 대시보드에 경고로 뜨는 상태 — 실제 홈택스 업로드는 사용자가 직접 진행해야 함)

### 실제 양식 분석으로 확정된 사항
- 세금계산서 "공급자" = 발전소가 아니라 **거래처(예: 키스트론) 단위로 고정** → `ClientGroup` 모델 신설, `PlantMaster`의 owner_* 필드 제거
- "공급받는자"(한전) 정보도 전 발전소 공통 고정값(종사업장번호·담당이메일만 발전소별로 다름) → `KepcoBuyerInfo` 단일 설정 모델 신설
- 마감일: PDF에 "8/25까지 세금계산서 발행" 형태로 명시 — 메일 수신월 25일이 마감으로 확인됨
- `발전소명 매칭데이터_세금계산서.xlsx`가 PRD가 요구한 "발전소명 자동 매칭" 테이블 그 자체(별칭↔공식명↔순번)

## Phase 2 — 통합 매출 데이터 리포트

- [x] 발전소별 뷰 (`/reports/plants/[id]`) — 귀속월별 발전량·발전시간·SMP단가·SMP금액·REC수량·REC금액·매출총액·청구상태
- [x] 거래처별/통합 뷰 (`/reports`) — 귀속월×거래처 매출 합계 + 전체 합계, 실제 32개 발전소 데이터로 검증(7월 합계 160,733,411원)
- [x] 청구 상태 표시 — 미청구/SMP확정/확정 배지. REC 데이터가 아직 없어 현재는 미청구·SMP확정만 발생(정상 동작)
- [ ] 엑셀 내보내기 — **보류: 기존 리포트 엑셀 양식 나중에 전달받기로 함**

## Phase 3 — REC 데이터 관리

- [x] 대표 단가 입력 (`/rec`, 귀속월별)
- [x] 발전소별 수량 입력 — 발전소 행마다 인라인 입력/저장
- [x] 발전소별 단가 수정 — "전체 발전소에 적용" 버튼으로 대표단가 일괄 반영 후, 행별로 개별 오버라이드 가능. 실제로 대표단가 적용→개별 수정→`/reports`·`/reports/plants/[id]` 합산 반영까지 검증 완료(테스트 데이터는 정리함)

## Phase 4 — 확장

- [x] 클라우드 배포 — Vercel(boyun/bodado)에 배포 완료. DB는 SQLite → Neon Postgres로 전환하고 기존 데이터(발전소 49건 등) 이관 완료. PDF 생성은 프로덕션에서 `puppeteer-core` + `@sparticuz/chromium`으로 전환(로컬 개발은 기존 puppeteer 그대로 사용). 프로덕션 빌드는 Turbopack이 `outputFileTracingIncludes`를 아직 지원하지 않아 `next build --webpack`으로 고정. 서버리스 Chromium에 한글 폰트가 없어 Pretendard를 PDF에 base64 인라인 임베드해서 해결(pre 태그 구간은 브라우저 기본 monospace로 빠지는 것도 별도로 고쳐야 했음)
- [ ] 거래처 자동 리포트 발송
- [ ] 다중 사용자/팀 협업

---

## 지금 막힌 항목 (사용자 확인 필요)

1. (선택) 기존 리포트 엑셀 양식 샘플 — Phase 2 엑셀 내보내기용

## 해결된 항목
- ~~한전 PDF 샘플~~ → 분석 완료 (메일 헤더+본문이 그대로 PDF화된 형태로 확인)
- ~~한전 세금계산서 등록양식(.xls) 샘플~~ → 2건 분석 완료, 컬럼 매핑 확정
- ~~세금계산서 마감일 기준~~ → PDF에 "8/25까지 발행" 형태로 명시, 메일 수신월 25일로 확인
- ~~영세율(02) 여부~~ → 없음, 항상 01(일반) 고정
- ~~영수/청구 구분~~ → 항상 02(청구) 고정
- ~~기존 31개 발전소 마스터 등록~~ → 일괄 등록 완료 (계약번호 미확보분은 `PENDING-{순번}` 임시값)

## 남은 후속 조치
- [ ] 계약번호 미확보 30개 발전소의 실제 한전 계약번호를 발전소 마스터 화면에서 하나씩 채워 넣기 (지금은 `PENDING-N`)
- [ ] 용량(kW)·주소도 양산 외 30개 발전소는 비어 있음 — 후속 메일 수집되는 대로 자동으로 채워지거나, 수동 보완 필요
- [x] 키스트론 "포항BW" 발전소 등록 완료 (계약번호 5000034726, 종사업장번호 184 — 메일에서 재조회해 정확히 확인, 32번째 발전소로 추가) → 해당 SMP 데이터도 자동 연결됨(검토 필요 → 정상)
- [x] `/smp` 검토 필요 목록에 남은 14건(키스트론 외 다른 거래처): 사용자가 필요할 때 직접 등록하기로 결정 — 당분간 보류
- [ ] IMAP 스캔은 현재 수동 트리거(버튼 클릭) 방식 — 완전 자동(주기적 백그라운드 폴링)으로 바꾸려면 별도 스케줄러 필요
- [x] 2026-07 귀속월 세금계산서 5개 배치(총 32개 발전소, 포항BW 포함) `./output`에 실제 생성 완료 — `/invoices`에서 다운로드·발행완료 처리 가능
- [ ] `/invoices`에서 실제로 다운로드해서 홈택스 업로드 양식과 열어봐서 최종 확인 권장 (자동 검증은 과거 실파일과 셀 값 비교로 완료했지만, 엑셀 프로그램에서 직접 여는 건 아직 안 해봄)

## UI 수정 요청 반영
- [x] 화면 가로폭 축소 — 콘텐츠 최대 1152px, 중앙 정렬(헤더 포함)
- [x] "대시보드" → "메인"으로 메뉴/제목 텍스트 변경, 메인 화면 하단 설명 문구 삭제
- [x] 메인 화면의 마감 알림 카드 삭제 (대시보드 D-day 위젯 제거)
- [x] 발전소 건설순서 1부터 순차 재정렬(32개), 신규 발전소 등록 시 기본값도 자동으로 "마지막 순번+1"이 되도록 수정
- [x] 거래처 관리 화면에 "엑셀 업로드" 버튼 추가 — 클릭 시 모달에서 샘플 파일 다운로드 + 엑셀 업로드로 거래처 일괄 등록/갱신 가능 (기존 거래처명은 갱신, 신규는 생성, 필수값 누락 행은 건너뜀). 실제 업로드까지 테스트 완료
- [x] **발전소 관리 화면**에도 "엑셀 업로드" 버튼 추가 — 여러 발전소를 한 번에 등록/갱신(계약번호 기준 upsert). 거래처명은 등록된 거래처와 일치해야 하며, 없는 거래처명은 건너뛰고 알려줌. 건설순서를 비우면 자동으로 다음 순번 배정. 실제 업로드까지 테스트 완료 → **계약번호 미확보 30개 발전소(`PENDING-N`) 값도 이 기능으로 엑셀 일괄 수정 가능**
- [x] 다른 PC에서 개발 서버 접속 시 403/HMR 웹소켓 실패 — Next.js 16의 `allowedDevOrigins` 교차 출처 차단이 원인. `next.config.ts`에 LAN IP(192.168.0.141) 추가로 해결
- [x] **SMP 화면 다운로드 버튼** — "엑셀 다운" → "다운로드"로 이름 변경, 클릭 시 "엑셀로 저장"/"PDF로 저장" 선택 모달 표시. 매칭 성공/검토필요 구분 없이 전체 SMP 데이터 대상. 스캔 완료 시(또는 이미 데이터가 있으면) 버튼 활성화
- [x] **PDF를 원본 메일 그대로 인쇄** — 기존 자체 제작 PDF 레이아웃을 버리고, Puppeteer로 원본 메일 HTML을 그대로 렌더링하는 방식으로 전면 교체. `양식/2026-07` 폴더의 실제 파일과 표·강조색·각주까지 완전히 동일하게 나오는 것 확인. 메일 원본 HTML은 DB에 캐시하고, 없으면(과거 수집분) IMAP에서 자동으로 다시 가져와 캐시함. `pdfkit` 제거, `puppeteer` 도입
- [x] 일괄 PDF 생성 속도 개선 — 처음엔 46건에 3~5분 걸렸으나(브라우저 탭 순차 생성+"load" 대기), 탭 재사용 + 동시 처리 12개 + "domcontentloaded" 대기로 변경해 **18초**로 단축(약 12배)
- [x] 저장 위치 선택 — 이 PC(localhost)에서는 PDF는 폴더 선택(여러 파일 저장), 엑셀은 파일 선택 창이 뜸(File System Access API). 다른 PC의 http://LAN-IP 접속처럼 API 미지원 환경에서는 PDF는 zip파일로, 엑셀은 기본 다운로드 폴더로 자동 대체(안내 메시지 표시)
